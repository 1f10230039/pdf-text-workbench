# バージョンに上限を切っているのは、PyMuPDF のメジャー更新で get_text("dict") の
# 構造が変わると抽出結果が黙って変わるため（同じ入力から同じ出力が再現できなくなる）
pymupdf==1.28.2
Flask==2.3.2

# 抽出単位の xlsx 書き出し。
# openpyxl は使わない：ワークシート参照を絶対パスで書き、KH Coder のパーサが読めないため
XlsxWriter>=3,<4

# アイコンを作り直すときだけ使う（ui/make_icons.py）。実行には不要
Pillow>=10,<13

# 公開デモ（Render）用のサーバー。ローカルでは不要
# Flask の開発サーバー（app.run）を公開の場に出さないこと
gunicorn>=21,<24
